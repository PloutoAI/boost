---
name: Bug report
about: Something is broken or behaving unexpectedly
labels: bug
---

**Version:** (output of `boost --version`)
**OS:** (macOS 14, Ubuntu 22.04, etc.)
**Bun version:** (`bun --version`)
**`BOOST_HOME` (if non-default):**
**`CLAUDE_CONFIG_DIR` (if non-default):**

### What happened

### What you expected

### Reproduction steps

1.
2.
3.

### Logs / output

> ⚠️ `boost --debug` may include file paths and structured metadata. It does not include prompt or completion content, but please scan output before sharing publicly.
